# 버니 길드 관리 — 개발 노트 (WORKLOG)

> 장소 바꿔서 작업할 때 혼선 안 생기게 하는 인수인계 문서.
> **새 장소에서 시작할 때: 이 파일부터 읽기 → `git pull` → 작업.**

배포: https://hsb9912.github.io/guild-manager/ (GitHub Pages)
저장소: https://github.com/HSB9912/guild-manager (소유 계정 **HSB9912**)

---

## ⚠️ 작업 시 필수 체크리스트
- [ ] **다른 PC면 항상 `git pull` 먼저** (옛 로컬 폴더는 버리고 새로 clone 권장)
- [ ] **push 권한은 `HSB9912` 계정에만** 있음. 다른 계정으로 로그인돼 있으면 `gh auth switch` 로 HSB9912 전환 후 push.
- [ ] **`assets/app.js` 또는 `assets/bunny.css` 를 고쳤으면 → 모든 `*.html`의 캐시버전 `?v=YYYYMMDD?` 를 일괄 bump** 해야 배포에 반영됨.
      (예: `?v=20260623b2` → `?v=20260623c2`. 안 올리면 사용자 브라우저가 옛 파일 캐시함)
- [ ] **사용자 체감 변경을 했으면 → `app.js` 상단 `CHANGELOG` 배열 맨 위 블록에 항목 추가** (요청 없어도). type: feat/fix/tweak/chore.
- [ ] **미리보기/시안 파일(`_preview/…`)은 커밋하지 않음** (로컬에서 브라우저로만 확인).
- [ ] 변경 후 `node --check assets/app.js` 로 문법 확인.

---

## 🏗️ 구조 한눈에
- **SPA**: 각 `*.html`은 `<div id="app" data-page="KEY">` 하나만 두고 `assets/app.js`(약 4천 줄, 단일 파일)와 `assets/bunny.css`를 로드.
- **페이지 빌더**: `app.js`의 `PAGES[key] = async ()=>html`. 메뉴/권한은 `GROUPS`/`META`. 관리자 전용 그룹은 `admin:true`.
- **백엔드**: Supabase(무료 플랜, DB+구글로그인) + Cloudflare Workers 5종(R2 이미지/보석금알림/장기부재 cron/매애기 프록시).
- **3길드(팩션)**: 버니(메인)/늑대/쿠거 = DB키 뚠/뚱/밤카롱. `GUILD`/`FACTIONS`로 전환.
- **로그인**: 구글 OAuth. 관리자 판정은 `admin_whitelist`/`admin_auth` 테이블.

### 자주 만지는 위치 (app.js)
- `CHANGELOG` 배열: 상단 (업데이트 내역 데이터)
- `GROUPS`/`META`: 메뉴 정의
- `buildMembers`/`memberGroups`: 길드원 탭
- `buildSuroInput`/`siBody`류(`_si*`): 수로 입력(실시간)
- `buildTodos`/`_todo*`: 운영진 할 일
- `render()`/부트: 파일 맨 아래

---

## 📓 작업 로그

### 2026-06-23 (집 + 다른 장소 연속 작업)
오전(집) — 설정 폼 편집기, 길드 개편 일괄 적용, 수로 보상 단위 버그픽스까지.
이후 세션 추가분:
- **운영진 할 일**: 사진 **Ctrl+V 붙여넣기**(작성/편집칸 둘 다), 글마다 **작성자 이름·작성일** 표시.
- **업데이트 내역(CHANGELOG)** 기능 신설: 새 버전 첫 접속 시 **우하단 What's New 팝업** + **사이드바 "업데이트 내역" 타임라인 모달**(C+A안). `localStorage bunny_clog_seen`로 본 버전 기록.
- **전체 모바일 최적화**(아이폰·안드로이드): 사이드바 → 슬라이드 드로어(햄버거 상단바), 카드 1열, 헤더/여백 축소. (카드 인라인 `grid span` 리셋로 2열 깨짐 해결)
- 사이드바 **정모 안내 박스 제거**(가짜 샘플이었음).
- **수로 입력 전면 개편**: 옛 일괄저장 표 → **실시간 동시 입력** 폼.
  - 셀 단위 **자동 저장**(충돌 없음), **Enter로 다음 칸**, 검색/미입력만/진행률, 지난주 점수 참고.
  - **Supabase Realtime**: presence(같이 작업 중인 운영진) + broadcast(점수 실시간 반영 + "누가 입력 중" 배지).
  - 견고화: 탭 복귀/25초 폴링으로 놓친 업데이트 보정. (무료 플랜 한도의 1% 미만)
- **길드원 탭 정리**: 전체/본캐/부캐 모드 + 정렬 드롭다운 제거 → **계정그룹 아코디언 단일**.
  - **헤더 클릭 정렬(C안)**: 활성 기준 핑크 강조 + ▲/▼ 토글. **전체 펼치기** 버튼.
  - 이름·직업 정렬 **유니코드 코드포인트순**으로 통일.

---

## 🔭 진행 상태 / 다음 할 일
- [ ] **수로 입력 실시간 — 목요일 5명 실전 테스트** 예정. 어색하면 정렬순/자동저장 타이밍/배지 문구 조정.
- [x] ~~메이플 스케줄러 API로 수로 점수 자동수집~~ → **불가 (재조사 금지)**. 아래 메모 참고.
- [ ] **헤더 클릭 정렬을 다른 표로 확장할지** 결정 (예: 수로 분석 점수 랭킹표). 현재는 길드원만 적용.
- [ ] **OCR(스크린샷 점수 인식) 홀드 중** — UI/UX 개편 끝나면 수로 입력 폼 상단에 "📷 스크린샷 인식" 버튼으로 복귀 예정. (자원은 `ocr/` 폴더에 있음)
- [ ] (검토) **`app.js` 단일 4천 줄 → 모듈 분리** 고려. 수정 비용↓·속도↑. 초기 작업 필요.

---

## 결정/합의 메모
- 메인 시안 톤: 버니 핑크(파스텔), 한글 위주.
- 동시 입력은 **실시간 동기화**로 확정(폴링/수동 대신). 비용 무료 한도 내.
- 정렬 UX는 **C안(헤더 클릭 + 활성 강조)** 확정.
- 미리보기는 항상 **로컬 브라우저로 직접 열어** 확인(서버 불필요), 커밋 안 함.

---

## ⛔ 조사 완료 — 다시 파지 말 것 (토큰 낭비 방지)
**메이플 스케줄러 Open API로 길드 전체 수로 점수 자동수집 → 불가능.**
- 엔드포인트: `GET /maplestory/v1/scheduler/character-state` (2026-06-25 출시). 응답에 `weekly_contents[]` 중 `content_name:"[길드] 지하 수로"` 의 `now_count` = 수로 점수. **데이터 자체는 정확** (너굴님 6/24 = 234,655 확인).
- 파라미터: `ocid` (실시간=오늘) / `ocid`+`date=YYYY-MM-DD` (과거, 최대 14일, 리셋 직전날로 지난주 점수 조회 가능). 오늘 날짜를 date로 주면 400 → 오늘은 date 빼고 실시간.
- **치명적 제약(넥슨 의도):** 공지 명시 "**자신의 계정 내 캐릭터**만 조회". 즉 API 키 주인 계정의 캐릭만 나옴. 길드원 637명 스캔 시 키 주인(너굴님) 1명만 조회됨. → **한 키로 길드 전체 자동수집 불가.** 멤버 각자 키 등록은 비현실적.
- 결론: 수로 입력은 **실시간 수동 입력**이 메인. 스케줄러 자동수집·OCR 모두 홀드/폐기.
